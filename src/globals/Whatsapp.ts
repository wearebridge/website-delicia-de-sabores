import { GlobalConfig } from "payload";


export const Whatsapp: GlobalConfig = {
    slug: "whatsapp",
    label: "WhatsApp",
    fields: [
        {
            name: 'whatsapp',
            label: 'Número',
            type: 'text',

        }
    ],
}